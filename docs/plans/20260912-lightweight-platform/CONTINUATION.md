# Continue the lightweight Iris product work

This is the handoff for the existing Iris task, not a new stopping criterion.
Continue implementation and proportionate verification until the user journeys
in `ACCEPTANCE.md` work. Do not keep rebuilding inactive components while the
actual install and highlighting paths remain unfinished.

## Ownership and fixed boundaries

- Integration workspace: `/Users/akrit/Documents/iris-lightweight-20260912`,
  branch `codex/iris-lightweight-20260912`. Start from its current clean HEAD,
  not an older task cwd or historical lab branch.
- Mann's draft PR https://github.com/Blueturboguy07/iris/pull/1 and
  `/Users/akrit/Documents/iris-pr-review-20260912` at `080ba93` remain frozen.
- The dirty historical `/Users/akrit/Documents/iris-harness-lab` is evidence
  only. Do not clean, reset, stash, or run feature edits there. Preserve all
  user Kneecap copies and normal Iris state.
- Terra High owns integration and reviews. Luna Medium/High implements
  bounded assignments in isolated worktrees. Escalate a specific difficult
  contract to Terra once; do not add a new orchestrator or repeated agents to
  the same task. Root remains mission control.
- Headless fixtures can run concurrently. One named owner uses the actual
  desktop and Xcode GUI. Never run terminal `xcodebuild`. Do not spend model
  calls on fixture-only checks or regenerate an already accepted artifact to
  repeat update/Undo.
- Jarvis and the faded game work remain paused. Do not restart the old noisy
  scheduled monitor. Report a meaningful milestone or a concrete request for
  user action, not unchanged status.

## What is already implemented and what the evidence means

| Area | Current result | Remaining user outcome |
| --- | --- | --- |
| Instruction/context bloat | Recurring AGENTS bytes fell from 115,517 to 12,027. Exact originals and invariant mapping retained. | Actual provider-token savings have not been measured. Preserve the short contracts. |
| Catalog and Ask | Published `guideSlug` decoding fixed. Visible refresh and last-check status use the bounded existing service. Ask stays general. | Real public-catalog/icon acceptance is separate from Test registry inventory. |
| Guide feedback | Real UI found and fixed silent opening failure, then unreadable failure text. Final native screenshots prove readable explanation and Dismiss. | A refusal is not a completed Kneecap setup. See `NATIVE_ACCEPTANCE.md`. |
| Source staging | Fixed Git operations create and record an isolated linked worktree, preserve dirty original files/index/HEAD, and reject wrong origin/pin/path. Executor cancellation, environment, overlap, output and timeout checks pass. | Choose and bind it through the real setup flow, watch, retry and resume. |
| Guide workspace schema | `workspace: { kind: "prepared-project", relativePath: "." }` or a nested safe path is decoded. Contradictory/malformed metadata is refused. | Runner deliberately refuses this new metadata until a validated binding is passed. No HOME fallback is allowed. |
| Spatial | Semantic fingerprint, real focused-window evidence, hashed labels, ambiguity/freshness results and coordinate transforms are tested. Eye memo receives the fingerprint. | Add the click-through outline and actual reacquisition. Test moving windows/tabs and stale-result suppression through the UI. |
| Mobile | Dependency-free install hub passed real 390x844 browser use, keyboard controls, offline warning persistence and a real Setup guide link. Eight Node tests passed. | It is a prototype. Signed native destinations, live capability catalog and physical-device use are not proven. |
| Versions | Strict bounded accepted-candidate metadata store and parent-symlink refusals passed tests and review. Existing receipt ownership is reused. | No new delivery or deletion authority exists. Bind actual persisted evidence and live source before reuse; then implement safe retention and UI acceptance. |
| Routing and complex edits | Usage reports configured arm, reservation/attempt, settled ledger and explicit unknown provider/UI state. A retained complex failure has a causal analysis. | Correct and verify repeat-import identity, then run one bounded real feature trial. No complex feature was accepted this phase. |

## Next implementation order, without blocking independent work

### First user milestone: Kneecap setup with accurate guidance

Integrate I2 and S2 before starting another broad harness campaign. These share
the desktop journey, but their pure logic can remain independently tested.

I2 owner files: `GuideSessionController.swift`, `GuideAutopilotRunner.swift`,
the existing guide loader/step metadata, source-workspace service/store, and
the existing setup/guide UI. Preserve step metadata through every copied or
repaired step. Add a typed optional workspace binding to the existing runner
path and resolve `relativePath` through `GuideSourceWorkspace`, not command
string substitution. Revalidate the bound guide revision, source identity,
stage ownership and directory before run/retry/repair/long-running execution.
Cancellation and late results must not bind a different guide or resumed run.

The source-only workspace store is the registry for this setup. Do not fake a
Mac `.app` entry in `IrisTestProjectRegistry`; that registry governs installed
Mac fixture edits and requires a real bundle. A future build/delivery needs
its own valid registered artifact before the existing delivery gates apply.

The current public Kneecap guide is version 5, pilot, pinned to
`fc48ba487a1e0d0cd10b30d6600acd2895ffdbed`, and still uses `~/kneecap` in command
text. Fetch and recheck its current content before changing its contract.
Create a versioned, owned Test guide with structural workspace metadata for
the scoped source setup. Use a narrowly validated Test route if needed; do
not globally enable marketplace autopilot or run arbitrary guide commands.
Preparing a workspace does not fix a later hard-coded command. The declared
guide and all command/dependency/watch paths must agree on the bound root.

Prove through actual Iris Test controls: pick Kneecap, detect the existing
dirty matching source, choose the isolated setup, continue from the prepared
root, cancel/retry without duplicate workspaces, and get the real device next
step. Use disposable registered fixtures for mutations. Preserve actual user
projects; do not hide the needed phone signing/distribution step.

S2 owner files: `GuideSessionController.swift` pointing callbacks,
`CompanionManager.swift`, `OverlayWindow.swift`, existing highlight/eye UI
and the already integrated S1 types. Coordinate these shared files with the
I2 owner before merging. Draw a small click-through outline around a current
target. Keep the existing eye and dark palette. Use existing generation
guards and bounded local observation. Re-resolve on relevant tab/window/app
change, clear ambiguous/stale/offscreen targets, and discard cancelled late
results. No continuous screenshots or model polling. A fresh rectangle alone
must never certify semantic identity.

Test the real setup guide and a few controlled app windows: duplicate labels,
same rectangle after changing tab, moving/minimizing the target window,
foreground-app change, permission prompt and cancellation. Actual desktop
tests are serialized even if their pure units run in parallel.

### Parallel version and cost work

V2 starts from the inert `AcceptedCandidateRecord` API. Its current
`revalidateAcceptedCandidate` compares supplied registry/receipt metadata and
the actual artifact digest. It does not inspect current Git origin/HEAD/branch
or dirty state. UUID shape is not positive review or UI evidence. Before any
delivery authority, load the exact persisted receipt under the existing store
boundary, resolve actual verifier/reviewer/UI evidence, and revalidate the
current source and registered paths. Reuse existing coordinator/recheck seams.

Only then add retention preview and bounded cleanup under the same store
ownership. Protect current, prior working rollback, pending/archived recovery
and unknown references. Keep tiny receipt history and user data. Refuse
symlink/alias overlap, stale previews or partial uncertain state. Measure
logical and allocated bytes separately. User acceptance: reuse one accepted
candidate to update while working, restart, Undo and confirm note data and
recovery remain available; show reclaimed/retained bytes truthfully.

H2 starts with `HARNESS_FAILURE_ANALYSIS.md`. Reverse-consumer selection and
false/error compaction already exist; do not redo them. Trial 21 imported the
same unchanged data repeatedly and grew folder counts 3, 4, 5, 6. Its 147
generated checks missed that multi-run state. Correct the app's transfer
identity/provenance contract in the existing maker/reviewer context, including
actual `Dashboard` and `NoteView` consumers. Do not build a generic lineage or
orchestration framework into Iris.

Freeze repeat-after-open/restart, destination collisions, same labels with
distinct origins, changed same-origin content, array permutations, onward
export, and malformed/dangling references as the behavioral oracle. After a
relevant fix, run one bounded real app feature journey with normal language.
Record all physical calls, retries, input/cache/output/reasoning fields, wall
time, failures and accepted outcome. Keep cached/reasoning subset counts from
being double-counted. Missing provider model/price/usage is unknown. Do not
claim a faster or cheaper model from selection labels or elapsed speed alone.

### Independent mobile next step

Keep `iris-mobile/` as the lightweight entry hub. It is not another Capacitor
wrapper around Kneecap, which already has one. First integrate verified route
and capability metadata with the current public catalog. An iPhone flow needs
an actual supported signed distribution route; the hub cannot eliminate that
platform requirement. Do not add a fake Install button or pretend an absent
web editor is runnable. Run the real mobile view against route changes and
failure states; an eventual physical phone test remains a distinct milestone.

## Evidence and callbacks

Read `IMPLEMENTATION_REVIEW.md`, `NATIVE_ACCEPTANCE.md`,
`MOBILE_ACCEPTANCE.md` and the matching work order before touching a subsystem.
Their historical checks are already complete. Re-run only affected checks and
one combined build after integration, then the relevant real UI journey.

Retained paths:

- I1 final Swift 6 fixture checks:
  `/Users/Shared/iris-source-workspace-build-ACHxdd/swift6-run.log`.
- S1 native module and 68 guide regressions plus spatial checks:
  `/Users/Shared/iris-harness-host-s1-window-evidence-20260912/`.
- I2 strict schema:
  `/Users/Shared/iris-guide-schema-build-Mc8jhJ/schema-run.log`.
- V1 final compile and inert checks:
  `/tmp/iris-harness-host-candidate-symlink-20260912/`.
- Combined integration: 177-source compile, inert checks, 69 guide tests and
  spatial checks passed in
  `/Users/Shared/iris-harness-host-lightweight-final-6InQWI/`. The Xcode GUI
  build succeeded at 11:11 AM Pacific. The launched Test identity and composer
  screenshot are recorded in `NATIVE_ACCEPTANCE.md`.

The currently running app is the DerivedData Test path in that evidence file,
not `/Applications/Iris Test.app`. The mobile prototype server is already
running on `http://127.0.0.1:4177/`, with an in-app browser deliverable tab.
Reuse it rather than launching another server on the occupied 4173 port.
Mission control releases foreground UI ownership when it dispatches this
handoff. Reacquire fresh CUA state; do not reuse another task's element IDs.

For a meaningful milestone, completion, or an actual need for user action,
send a short plain-language result back to mission control task
`01a08334-0a22-78b0-b2d6-fad06b5dc449`. Include exact code revision, tested vs
WIP, a file containing evidence, and the next concrete action. Never claim
that sending a task, a model's success text, a unit test or a build proves the
installed user experience.
