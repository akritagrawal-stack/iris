# Iris main app target

Follow parent contracts and the September 12 plan. Archived build numbers/test counts are history, not current installed evidence.

- Preserve `OverlayEyeDragSession` immutable anchors/fixed coordinates, animation-free final save shared with watchdog, common-mode pointer timer and edge recovery. Programmatic layouts never overwrite placement preferences.
- Loading is request-ID guarded; guide pointing cannot animate chat. Reduce Motion uses static feedback. Keep History/New chat reachable and archive scroll height bounded. Ask/Edit drafts and unsent attachments stay separately in memory.
- `ChatTranscriptStore` preserves conversation boundaries, not cross-chat warmup. Clear history is Cancel-default and chat-only; persistence failures stay visible. Tour remains optional/replayable with no unproduced video promise.
- `CodexEditModelSelection` reads public metadata only. Requested route is not provider identity. Rendering labels cannot authenticate. Codex edits and screen-help transports remain distinct.
- `EditVerificationReceipt` separates passed/failed/skipped and delivery. A feature is not a verified bug fix. Run logs stay open through delivery/verdict until flow reset; output remains scrubbed/bounded.
- Preserve `KeychainReadPolicy` serialization/restoration of its process-local interaction flag; LAContext alone does not suppress legacy prompts. No background auth, failed-save deletion or transient-refresh wipe. Offline tests do not prove sign-in.
- Pending/stopped/archived recovery remains accessible after restart and protects affected work. Unknown targets block conservatively. Marker removal needs checked persistence. Metadata cannot prove restored app/source/behavior.
- Catalog compatibility preserves unknown support and bounded enrichment. Icon HTTPS/type/size/concurrency/cache limits remain. Guide availability cannot enable absent commands.
- Spatial freshness/cancel/ambiguity and traversal/model limits remain. Outline is click-through, focus-preserving and below required system UI. Geometry alone does not prove target or unchanged tab.
- AppKit panels use MainActor lifecycle/SwiftUI bridging. Exclude Iris floating controls from captures where required. Preserve close, minimize/reopen and optional edit-start-minimized behavior.

Relevant tests are listed in the parent. Exact historical recovery/account/panel contracts: `../../docs/history/agent-instructions-20260912/README.md`. Keep dated status and line-count tables out of this file.
